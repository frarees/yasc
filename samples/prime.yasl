integer n
integer d
n := 2

while(n < 100)
    d := 2

    while(d * d <= n && n / d * d != n)
        d := d + 1
    end

    if(d * d > n)
        print(n +" is prime. ")
    else
        print(n +" isn't prime. ")
    end

    n := n + 1
end
